apiVersion: v1
kind: Service
metadata:
  name: iam-service
spec:
  type: ClusterIP
  selector:
    app: iam-service
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
    - name: grpc
      protocol: TCP
      port: 5001
      targetPort: 5001
