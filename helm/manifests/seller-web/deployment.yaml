apiVersion: apps/v1
kind: Deployment
metadata:
  name: seller-web-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seller-web
  template:
    metadata:
      labels:
        app: seller-web
    spec:
      serviceAccountName: hacmieu-sa
      containers:
        - name: seller-web
          image: public.ecr.aws/h5u5b6t3/hacmieu/seller-web:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: hacmieu-secrets
          ports:
            - containerPort: 3001
          resources:
            requests:
              cpu: '250m'
              memory: '512Mi'
            limits:
              cpu: '500m'
              memory: '1Gi'
