apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-web-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-web
  template:
    metadata:
      labels:
        app: customer-web
    spec:
      serviceAccountName: hacmieu-sa
      containers:
        - name: customer-web
          image: public.ecr.aws/h5u5b6t3/hacmieu/customer-web:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: hacmieu-secrets
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: '250m'
              memory: '512Mi'
            limits:
              cpu: '500m'
              memory: '1Gi'
