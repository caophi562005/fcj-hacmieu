{
  "name": "@fcj-hacmieu/convex-backend",
  "version": "0.0.0",
  "private": true,
  "description": "Convex backend (schema + functions). Đặt cùng folder với thư mục `convex/` để `npx convex dev` chạy được khi cwd ở đây.",
  "dependencies": {
    "convex": "^1.37.0"
  }
}
